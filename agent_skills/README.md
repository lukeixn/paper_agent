# Agent Skills

Markdown skills uploaded from the UI are stored in one directory per Agent:

- `survey_agent/`
- `innovation_agent/`
- `method_agent/`
- `limitation_agent/`

Each Agent loads only the Markdown files in its own directory.
