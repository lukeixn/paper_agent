# Runtime API keys should be provided by the UI or environment variables.

MODEL:
  PROVIDER: deepseek   # offline / openai / deepseek

  OPENAI:
    API_KEY: ""
    MODEL_NAME: "gpt-4o"

  DEEPSEEK:
    API_KEY: ""
    BASE_URL: "https://api.deepseek.com"
    MODEL_NAME: "deepseek-chat"

  TEMPERATURE: 0.3

AGENT:
  MAX_STEPS: 5

SEARCH:
  MAX_RESULTS: 10
